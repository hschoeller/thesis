% Encoding & fonts
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{microtype}
\usepackage[utf8]{inputenc}

% Language
\usepackage[english]{babel}
\usepackage{csquotes}
% Math & symbols
\usepackage{amsmath,amssymb,amsthm,mathtools}
\usepackage{dsfont}
\newcommand{\R}{\mathds{R}}
\newcommand{\N}{\mathds{N}}
\newcommand{\C}{\mathds{C}}
\newcommand{\Z}{\mathds{Z}}
\newcommand{\E}{\mathds{E}}
\newcommand{\norm}[1]{\left\lVert#1\right\rVert}
\newcommand{\abs}[1]{\left|#1\right|}
\newcommand{\1}{\mathds{1}}
\renewcommand{\P}{\mathds{P}}
\newcommand{\ran}{\textup{ran}}
\newcommand{\Id}{\textup{Id}}
\newcommand{\dd}{\mathrm{d}}

\newenvironment{nalign}{
    \begin{equation}
        \begin{aligned}
            }{
        \end{aligned}
    \end{equation}
    \ignorespacesafterend
}
\theoremstyle{definition}
\newtheorem{remark}{Remark}

% Figures, tables, cross-referencing
\usepackage{graphicx}
\graphicspath{{fig/NPG/}{fig/CAMCOS/}{fig/WCD/}}
\renewcommand{\floatpagefraction}{0.75}
\usepackage{booktabs}
\usepackage{caption}
\usepackage{subcaption}
\usepackage{float}

% Bibliography
\usepackage[
    backend=biber,
    style=authoryear,
    maxcitenames=2,      % et al. after 2 authors in citations
    maxbibnames=999,       % show up to 999 authors in bibliography
    uniquename=false,    % don't add initials to disambiguate
    uniquelist=false,    % don't expand et al. to disambiguate
    date=year,           % year only, no full date
]{biblatex}
\addbibresource{bib/manual-fixes.bib}
\addbibresource{bib/references.bib}

% Remove URL and visited-on fields
\AtEveryBibitem{
    \clearfield{url}
    \clearfield{urldate}
    \clearfield{urlyear}
    \clearfield{urlmonth}
    \clearfield{urlday}
}

\DeclareSourcemap{
    \maps[datatype=bibtex]{
        \map{
            \step[fieldsource=doi, final]  % only proceed if DOI exists
            \step[fieldset=url, null]
            \step[fieldset=urldate, null]
            \step[fieldset=urlyear, null]
            \step[fieldset=urlmonth, null]
            \step[fieldset=urlday, null]
        }
    }
}

% Microtypography
\usepackage{microtype}

% Custom macros
\usepackage{style/thesis}

% Structure
\usepackage{appendix}
\usepackage{enumitem}

% Figures with TikZ
\usepackage{tikz}
\usepackage{standalone}
\usetikzlibrary{arrows.meta}
\usetikzlibrary{positioning,calc}

% Hyperlinks & references
\usepackage[hidelinks]{hyperref}
\usepackage[nameinlink]{cleveref}
% use ch, sec, eq, fig, tab as prefixes for cross-references

% Acronyms
\usepackage[acronym, nomain]{glossaries}

% Initialize glossary
\makeglossaries

\input{acronyms/acronyms}

% Metadata
\title{Atmospheric Blocking and Warm Conveyor Belts:
    Mathematical Perspectives}
\author{Henry Leon Schoeller}
\date{\today}
